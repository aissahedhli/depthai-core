# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-icansii-distro"
BBFILE_PATTERN_meta-icansii-distro = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-icansii-distro = "6"

LAYERDEPENDS_meta-icansii-distro = "core"
LAYERSERIES_COMPAT_meta-icansii-distro = "kirkstone"
